{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Stars App Server",
            "program": "${workspaceFolder}/index.js",
            "env": {
                "PORT": "8000"
            }
        }
    ]
}